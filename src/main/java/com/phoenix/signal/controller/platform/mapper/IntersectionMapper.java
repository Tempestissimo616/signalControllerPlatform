package com.phoenix.signal.controller.platform.mapper;

import com.phoenix.signal.controller.platform.model.Intersection;
import com.phoenix.signal.controller.platform.utils.basicController.BasicMapper;
import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface IntersectionMapper extends BasicMapper<Intersection> {
}
